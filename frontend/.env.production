# Variables de entorno para PRODUCCIÓN.
# Cuando Nginx hace el Reverse Proxy, el frontend no llama a localhost:8000
# sino que llama a su propio dominio bajo el path /api (ej. midominio.com/api)
# Esto soluciona por completo los errores de CORS.
VITE_API_URL=/api
